server:
  port: 9005
  
spring:
  application:
    name: HASTRIX-DASHBOARD
  cloud:
    config:
      enabled: true
      uri: http://localhost:9006
    
hystrix:
  dashboard:
    proxy-stream-allow-list: "*"
